// LEGACY: This page is no longer used. The canonical creator flow is ScenarioBuilder (/creator/new).
// Kept only to avoid import/runtime errors if referenced.
import React from "react";

export default function ScenarioDetailPage() {
  return (
    <div className="p-6 text-muted-foreground text-sm">
      This view is deprecated. Please use the new creator flow.
    </div>
  );
}
